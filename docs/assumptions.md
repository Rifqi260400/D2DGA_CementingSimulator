# Assumption register

One row per modelling choice not fully pinned down by the six source papers.
Updated as the build proceeds. IDs are referenced from code comments.

Status key — **Conflict**: sources disagree, a choice was forced.
**Silent**: sources agree but are silent. **Pending**: awaiting user data.

---

## Convention conversions (build spec §1.1)

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| CONV-01 | Stream-function definition | `∇aΨ = (2Hw̄, −2Hv̄)` | **Conflict.** B02 (39) and PF04 (5) use `∂Ψ/∂φ = r_a H w̄` — no factor 2. BF25 §2.1, ZF22 (2.2), ZF23 (2.7) carry the 2. | Spec §1.1 mandates BF25. Everything taken from B02/PF04 is rescaled at the point of use. Getting this wrong puts every velocity out by 2. | Enforced by M4-T4 (flux through any ξ-slice = Q) |
| CONV-02 | Buoyancy scalar | BF25 (2.12), `b = −Δρ̂/(r_a Fr*²)`, `Δρ̂ = ρ̂₁ − ρ̂₂`, `b > 0` for denser-displacing-lighter upward | **Conflict.** B02/PF04 use a Stokes number `St*`; ZF22 uses `b = Δρ̂ĝd̂²/(µ̂₁ŵ₀)` with the *opposite* density difference sign; BCF25 (24) uses a geometric-mean viscosity. | Spec §1.1 mandates BF25. ZF22 conversion is exercised at M1-T2. | M1-T2 (pending) |
| CONV-03 | `𝓘₃` — magnitude **and sign** | **`𝓘₃ = −c²(1−c)³[4mc+3(1−c)] / (12√m [mc³+1−c³])` — NEGATIVE on 0 < c̄ < 1.** | **BF25 (2.27) as printed is wrong three times over.** | Integrating BF25's own (2.23) with `η₁=√m, η₂=1/√m` gives the form at left, exactly, for all `m` and `c̄`. Print differs in the numerator (`3(1−c)` not `3(1−c²)`), in the denominator (a missing `√m`, although the neighbouring (2.24)–(2.25) both carry theirs) — and in the **sign**. The first two are transcription slips. ⚠️ **The sign is the one that changes results, and an earlier version of this code had it backwards.** The magnitude had been confirmed three ways, so the error was invisible to every static check; it only surfaced when the coupled loop ran ZF22 case 4 (`b = 1000`, the most strongly buoyant case) and returned a violent counter-current mush instead of a near-piston displacement. **Three independent confirmations of the sign**, all now asserted: (1) (2.23) integrated symbolically — and the *same* computation reproduces the printed (2.24) and (2.25), so the layer convention and the algebra are pinned by the same check, not assumed; (2) BF25 §3.2 in words — *"the shock velocity approaches the mean velocity (w_f → 1), and the front becomes more stable when b ≫ 1 … There is less dispersion for larger b"*, and *"buoyancy completely suppresses the spike regime as b increases from 100 to 1000"*; with the opposite sign larger `b` would give a flux hump many times the mean flux and a violent exchange — the exact opposite; (3) ZF22 Table 3 case 1, the one case with `b < 0`, has by far the worst displacement of the ten (`t_br = 0.44`), which is what a sign-flipped buoyancy flux produces. Conversions (the other papers print the magnitude under their own sign conventions, so the factors carry the minus): **BCF25 (23) = −MASTER·√m**, **ZF22 (4.26) = −MASTER·6/√m**. | **Yes — `test_m2_t3_cross_paper_reduction_and_conversion_factors` (symbolic, general m) and `test_conv03_sign_is_what_makes_buoyancy_stabilising`, which tests BF25 §3.2's *physical* statement: the leading front speed `max f(c̄)/c̄` falls monotonically to 1 as `b` grows, and the spike width collapses like `2√m/b`** |
| CONV-04 | `r_a` in the elliptic buoyancy scalar | Derived `r_a`-explicit form: `B_φ = bs·cos β`, `B_ξ = bs·r_a sin β sin πφ`, `bs = (1/Fr*²)[1 − Δρ̂(c̄ + 𝓘₂/𝓘₁)]` | **Conflict, newly identified.** The symbol `b` in BF25 (2.12) and in BF25 (2.18) differ by a factor `r_a`. See `docs/derivation.md` Finding 2. | At `r_a = 1` (every published case) the two agree, so the paper cannot distinguish them; under a caliper wall `r_a` varies and they diverge. **Now confirmed by a second, independent route**: cross-differentiating BF25 (2.11)–(2.13) directly to eliminate the pressure — without going through (2.16)/(2.18) at all — reproduces exactly these `B_φ`, `B_ξ`, with the `r_a` of (2.12)'s `b` cancelling against the `r_a` in `f`. Note this is the *opposite* outcome to CONV-10 in the transport flux, where one `r_a` survives — which is why each had to be derived rather than pattern-matched. | Derivation is symbolic and exact; asserted in M4 |
| CONV-10 | `r_a` in the **transport** buoyancy flux | **Derived form `q_buoy = r_a H³𝓘₃ G_b`** — one factor of `r_a`, the same as the advective flux. | **Conflict with all three papers as printed.** BF25 (2.21), BF25 (3.3) and BCF25 (5)+(24) all print it so that `r_a` **cancels**: BCF25 writes `b H³ r_a 𝓘₃` and then defines `b` with a `1/r_a` in it (24); BF25 writes `−b H³𝓘₃(f_ξ,−f_φ)` with the same `b` (2.12) and `f` carrying `r_a`. | That contradicts their own definition of the flux. BF25 (2.20) is `q = r_a(∫₀^{y_i}v dy, ∫₀^{y_i}w dy)`, and **(2.20)'s `r_a` is corroborated twice**: the *advective* term of both (2.21) and BCF25 (5) carries it, and without it (2.19) does not reduce to the correct material derivative `∂c̄/∂t + (v̄/r_a)∂_φc̄ + w̄∂_ξc̄ = 0`. The gap-scale integral it multiplies is `r_a`-free — the layer profile depends only on `G` and `G_b`, and `G_b` is itself `r_a`-free once (2.12)'s `1/r_a` cancels the `r_a` in `f`; `𝓘₃` (2.23) is built only from gap integrals. So the buoyancy flux must carry exactly **one** `r_a`. Every published case has `r_a = 1`, so no result in any of the three papers can distinguish the readings; under the K-GEP-1 caliper `r_a` varies by 65% and they differ by that much. Same family as CONV-04 and resolved the same way — by deriving from (2.11)–(2.13) and (2.20) rather than by choosing a paper. | Invisible at `r_a = 1`; asserted symbolically against (2.19)'s material-derivative reduction |
| CONV-05 | `Ψ(1,ξ,t) = Q(t)` from B02 (68) | Rescaled into CONV-01 convention before use | **Conflict** (consequence of CONV-01) | Same reason as CONV-01. | M4-T4 (pending) |
| CONV-06 | `1/Fr*²` term in `b` | Dropped for K-GEP-1 | **Silent→resolved.** BF25 (2.18) drops it citing `∇a·f = 0`. | `docs/derivation.md` Finding 1 shows `∇a·f = [r_a cos β dβ/dξ + sin β dr_a/dξ] sin πφ`, which is *identically* zero at β = 0 however `r_a` varies — a stronger justification than BF25's. **Must be retained for any inclined caliper well.** | **Yes — M0-T4 passes**, and the companion test confirms the diagnostic detects a non-zero value at β = π/4 |

---

## Geometry (build spec §3, §4)

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| GEO-01 | Casing OD | **177.8 mm (7 in)** | **RESOLVED from the casing programme.** | The programme is telescopic: 13⅜" conductor, 10" (ID) casing to the shoe, **7" liner** over the open hole. The 7" liner is the string cemented into 194–390 m, so its OD is the annulus inner radius. **Only the OD is needed** — the annulus is between casing OD and hole wall; casing ID never enters the model, so the missing ID column is not a gap. The earlier tension with the spec's ~0.12 δ/π stands but is now explained: 0.12 corresponds to a ~15.5 in hole, i.e. a washed section rather than gauge. | n/a — resolved |
| GEO-02 | Casing eccentricity | `e_gauge = 0.3` (≈70% standoff), imposed as a **constant physical offset** `ê = 12.95 mm` | **Decided by Claude; input still missing.** No K-GEP-1 centralizer record supplied. | Routine field value. Constant-`ê` is the spec-mandated default (§4) and is physically correct: `e = ê/(2d̂)` falls inside a washout because the casing does not move when the hole enlarges. Model is pluggable (`EccentricityModel` ABC). | **Yes — and the result is a warning.** Sweeping `e_gauge` 0.1→0.5 on the synthetic wall collapses the far-field narrow-side velocity fraction `(1−e)²/(1+1.5e²)` from **0.643 to 0.005**, a factor of **130**; on the washout wall 0.798→0.182. With constant offset, `e` reaches 0.537 at `e_gauge=0.3` and 0.895 at 0.5 — near contact. **This is the dominant control on predicted narrow-side displacement, so it is not a benign default: the centralizer record is required before any production claim.** Asserted in `test_geo02_standoff_dominates_narrow_side_velocity`. |
| GEO-03 | Synthetic amplitude basis | **Diameter** (`amplitude_is_diameter=True`) | **Conflict.** Spec §3.5 writes `gauge + A·sin(2πξ̂/L)` for the *radius*, but quotes "gauge hole 10.4 in" (a diameter) and "amplitude 1.5 in" side by side. | Reading both as diameters keeps them on the same footing. Radius reading leaves only a 0.2 in minimum gap at A = 1.5 in with 7 in casing. Flag exposed for the literal reading. | Switchable; not yet swept |
| GEO-04 | Synthetic wall mode | `symmetric` default, `enlargement` available | **Silent.** Spec writes a symmetric sinusoid but also demands "strictly positive annular gap everywhere" *and* a sweep to A = 6.0 in — mutually impossible. | **Quantified:** with 7 in casing at `e_gauge = 0.3` the symmetric form admits A < 2.38 in (`e < 1` limit) and A < 3.40 in (positive-gap limit). The spec's sweep {0.5, 1.5, **3.0**, **6.0**} in is **half infeasible**: A = 3.0 gives e = 2.55, A = 6.0 gives a negative gap. One-sided `enlargement` mode admits all four and is the physically honest model (holes wash out, they do not wash in). | **Yes — `test_m0_synthetic_wall_feasibility_boundary`** |
| GEO-05 | Caliper interpolation | Linear, clamped at ends | **Silent.** | Higher-order interpolation would manufacture detail the log does not resolve. Digitisation uncertainty ±0.3–0.5 in, features under ~0.5 m unresolved, possible clipping above 24 in. | Not applicable until the real log arrives |
| GEO-06 | Washout shape | Gaussian, centre ξ̂ = 184 m, σ = 5.0 m | **Silent.** Spec gives only "195–217 m reaching ≈23 in". | ±2σ spans depth 196–216 m, matching the stated interval. Smooth and C^∞, so it stresses the slow-variation assumption only through its gradient — which is what we want to measure — not through a discontinuity. | Not yet |
| GEO-07 | Washout δ/π expectation | Computed, **not** taken from spec | **Spec error.** Spec §3.4 states δ/π "rising to ~0.34 in the washout". | δ/π = (r̂_o−r̂_i)/[π(r̂_o+r̂_i)] is bounded above by **1/π ≈ 0.3183** for any r̂_i > 0, so 0.34 is unattainable by that formula. Computed value for a 23 in washout: **0.170** (7 in casing). Reported rather than reproduced. | n/a — arithmetic fact |

---

## Numerical (build spec §7)

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| NUM-01 (Q2) | CFL number | **0.5 default; transport half resolved.** | BCF25 says "< 1", never gives a value | **Measured, not assumed — and the result runs against the usual instinct.** On the m = 1 rarefaction against its exact solution the L1 error *rises* as the CFL number falls: **0.0031 at 0.95, 0.0079 at 0.5, 0.0102 at 0.25** — a factor of 3.3 across the range. That is the expected behaviour of a first-order upwind scheme (exact at CFL = 1 for linear advection), so "smaller timestep, safer" is wrong for accuracy here. Monotonicity does not constrain the choice either: BCF25 (44) is evaluated on `Ψⁿ` and `c̄ⁿ`, both known before the step, so a large CFL number is not a gamble — the maximum principle is asserted at **0.99**. What is still open is the *other* half: the elliptic/transport splitting error, which grows with `Δt` and is invisible while `Ψ` is frozen. Default left at 0.5 pending **M6-T4**. | **Yes — `test_q2_cfl_number_accuracy_and_monotonicity`, and M5-T2 at cfl = 0.99** |
| NUM-02 (Q3) | AL parameters `r`, `ρ`, tolerance | **Resolved: `ρ = r`, chosen by probing at table-build time (`TwoLayerGapSolver.tuned`). Tolerance 1e-9.** | PF04 gives only `0 < ρ < r(1+√5)/2`; BF25 A.2.3 reports "ρ = r = 1" and neither says how to choose it. | **`r = 1` is badly scaled once buoyancy is strong, and the cost is severe.** Measured on the K-GEP-1 fluid pair (`B = 11.9`, `m = 0.0064`): at `gb = 0`, `r = 1` takes 22 iterations and the whole range `r = 0.01…1` sits on a plateau near 20; at `gb = 27.9`, `r = 1` takes **3434 iterations (5.4 s)**, `r = 0.1` takes 392, and `r = 0.01` takes **56 (0.09 s) — 61× faster**. Below `r ≈ 0.003` it degrades again at both, so the optimum is a genuine minimum, not "smaller is better". The converged closures agree to **13–14 significant figures across the whole ladder**, which is the property that makes this an optimisation rather than a knob: `r` sets the *rate* and never the answer. **The optimum is fluid-pair dependent, which is what settles the design**: for a Bingham-Newtonian pair at the same `gb = 25`, `r = 1` is already near optimal and `r = 0.01` is ~20× *worse*. A single new fixed default would have traded one badly scaled case for another, so a closure table probes the ladder once at the largest `\|gb\|` on its own axis — a second or two, amortised over thousands of solves. Effect in practice: the K-GEP-1 735-point table went from **not finishing in 10 minutes to 180 s**. | **Yes — `test_num02_al_parameter_sets_the_rate_and_never_the_answer` asserts both halves: r-independence of the closures to 1e-10, and that the iteration count really moves** |
| NUM-03 (Q4) | Closure table resolution | *pending* | Silent | Refine until M3-T1 passes with margin | Pending |
| NUM-04b | Bottom-hole condition — **measured on K-GEP-1** | **B02 (70) leaks; the uniform-inflow condition closes the volume balance exactly.** | — | The volume-balance diagnostic (NUM-29) turns this from a judgement call into a measurement. Excess displacing fluid in the annulus over what was pumped, tracked through a K-GEP-1 run: **B02 (70) sits at a constant +0.0050 ± 0.0004 volumes**, while **uniform inflow sits at −0.0002 ± 0.0002 — zero to within rounding**. The mechanism is exactly the one predicted: B02 (70) imposes zero axial gradient and so *permits* `w̄ < 0` at `ξ = 0`; with upwinding at that face (NUM-29) the inflowing part carries `q₀(c̄_in) = 1` while the backflowing part carries `q₀(c̄₀) < 1`, so net influx exceeds `Q`. The uniform condition forbids backflow and the balance closes. ⚠️ **A real casing shoe does not admit backflow**, so this is evidence that the uniform condition is the physically correct one here, not merely the tidier one — which reverses the preference NUM-04 recorded for the published (stable, weakly buoyant) cases. The magnitude is small — 0.005 of 1.2 volumes is 0.4% — but it is *systematic*, and under FLU-06's 10³ buoyancy it was worth measuring rather than assuming. | **Yes — matched pair at 16 × 80, both conditions, tracked over 30%+ of the run** |
| NUM-04 (Q5) | Bottom-hole inflow condition | **`no_axial_gradient` (B02 70) — resolved.** `uniform` also implemented and selectable | B02 §3.2 calls its own choice uncertain | B02's justification (entry effects negligible in long annuli) is weaker here: their well is 1000 m, K-GEP-1's open hole is 196 m — so it was measured rather than assumed. **Result:** the two conditions differ by 22% of 2Q *at* the boundary but the difference falls below 1% of its peak within **1.3 m — 0.7% of the 196 m open hole**. The choice is therefore immaterial except immediately above bottom hole, and B02 (70) is kept. ⚠️ An earlier commit accepted `inflow='uniform'` and silently ignored it; then a second attempt appended the Dirichlet row *after* the flux stencil, and because scipy **sums** duplicate entries the row became flux+1 instead of an identity. Both fixed and guarded by tests. | **Yes — `test_q5_inflow_choice_is_local_to_bottom_hole`** |
| NUM-05 (Q6) | φ symmetry vs periodicity | Symmetry (default) | All published 2D work imposes symmetry | ZF22 §5 notes periodicity permits azimuthal asymmetry and is a small change. Hook to be left. | Pending |
| NUM-06 (Q7) | Initial interface at t = 0 | **Sharp step — resolved.** | Silent | The two candidates (sharp step, one-cell smoothed step) differ by an amount that decays at **first order in L1 — ratio 2.00 per mesh halving over three meshes, down to 6.3e-4** — i.e. at exactly the scheme's own accuracy. The initial shape is therefore a discretisation detail, not a modelling choice, and the sharp step is kept (it is also what BCF25 uses). | **Yes — `test_q7_initial_interface_shape_is_immaterial`** |
| NUM-07 | Grid aspect ratio | `n_phi=20, n_xi=400` (BCF25's) | BCF25 §IV | ⚠️ On K-GEP-1, Z ≈ 564, so `dξ = 1.41` against `dφ = 0.05` — a cell aspect ratio of ~28. BCF25's own case had Z much smaller. Flagged for M6-T3. | Pending — M6-T3 |

---

## Scaling and gap-scale solver (M1, M2)

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| SCA-01 | Radial scale `d̂*` | `d̂* = δ* · r̂*_a` | **Silent.** BF25 calls `d̂*` "a ξ-averaged half-annular gap width" without saying which average. | Forced, not chosen. B02 (21) defines H so that `H = 1` means a half-gap of `δ* r̂*_a`. An independently averaged `d̂` would make `H = 1` mean something else and break the reduction test M0-T1. For a uniform annulus the two coincide. | Implied by M0-T1 + M0-T2 |
| CONV-07 | ZF22 Table 2 `Re` column | Computed, **not** matched to print | **Spec expectation not met.** Build spec M1-T2 says Table 2 "must match to the printed precision". It does not. | Every case comes out at **0.954×** the tabulated Re — 19.05 vs 20, 95.4 vs 100, 954 vs 1000. A constant ratio across three decades is a rounding convention, not a scaling error, and it cannot be a `d̂` mismatch because the `b` column carries `d̂²` and reproduces to <1% with the same `d̂ = 2.385 mm` (stated explicitly in ZF23 §2.1). Conclusion: Table 2's Re holds rounded **design targets**. Harmless — the D2DGA model is non-inertial, so Re never enters the solution. | **Yes — asserted as a systematic ratio in M1-T2** |
| CONV-08 | ZF22 Table 1 case 9 `Q̂₀` | Driven from `ŵ₀ = 0.04`, implying `Q̂₀ = 2.38e-5` | **Source typo.** Table 1 prints `Q̂₀ = 2.38e-4`, but `Q/A = 0.400 m/s` contradicts its own `ŵ₀ = 0.04`. Case 10 prints the same Q with `ŵ₀ = 0.4`, which is consistent. | Case 9's `Re = 100` (vs case 10's 1000) independently requires `ŵ₀ = 0.04`. All cases are driven from `ŵ₀`, which is unambiguous. | Asserted in M1-T2 |
| ~~CONV-03~~ | **SUPERSEDED — see the CONV-03 row under *Convention conversions* above.** | — | — | ⚠️ **B-4.** This was a duplicate row with the same ID whose content CONTRADICTED the authoritative one: it printed the master form **without its leading minus** and gave the conversions as `BCF25 (23) = MASTER·√m`, `ZF22 (4.26) = MASTER·6/√m`, also without their minus signs, and described BF25 (2.27) as "wrong twice" rather than three times. Followed literally it reintroduces the sign error that CONV-03 exists to record. The signs are now settled from the primary sources and asserted: `test_b4_conv03_buoyant_flux_agrees_with_zf22_4_25` shows the code's buoyant flux matches **ZF22 (4.25)** to 3.9e-16 over 144 states once ZF22's own `Δρ = ρ₁ − ρ₂` (stated at its (4.24)) and `η₂ = 1/m` are used, and `test_b4_conv03_conversion_factors_carry_their_minus_signs` pins `BCF25 (23) = −√m·MASTER` and `ZF22 (4.26) = −(6/√m)·MASTER` to 1e-13. Both fail against the form this row described. | Superseded |
| ~~NUM-02~~ | **SUPERSEDED — see the NUM-02 (Q3) row under *Numerical* above.** | — | — | ⚠️ **B-4.** Duplicate ID with contradictory content: this row said `r = ρ = 1`, `tol = 1e-10`, `N_y = 400`, while NUM-02 (Q3) records the resolved choice — `ρ = r`, probed per fluid pair at table-build time (`TwoLayerGapSolver.tuned`), `tol = 1e-9`. The resolved row is the one the code implements. `N_y = 400` remains the `ClosureTable` default; the K-GEP-1 production table was built at `n_y = 200`, which is recorded under NUM-03. | Superseded |
| NUM-08 | Symmetry constant of integration in BF25 (A18)/(A26) | Set to **exactly zero** | **Silent — and a genuine trap.** (A18) fixes the constant with `−r q(0) + λ̃(0)`, but with `q`, `λ̃` at cell centres neither is available at `y = 0`. | Extrapolating from the first two centres is **O(dy) wrong** for `du/dy ~ y^{1/n}`: the fixed point absorbs the mismatch as a constant offset in `λ̃`, shifting the entire stress field. Predicted `λ̃ = −0.5717 G dy` for n=0.6; measured −0.01143 at G=4, dy=0.005, predicted −0.01143. Cost: 1.6e-3 error in `ū` and **first-order** mesh convergence, against the second order BF25 claims. Both `du/dy` and `τ` are **odd** about the channel centre, so the bracket is exactly zero. Fixing it recovered clean second order and improved the worst case **2500×** (1.6e-3 → 6.4e-7). | **Yes — M2-T5 asserts the order ratio ≈ 4** |
| NUM-09 | AL convergence test | `max(stagnation, feasibility)` | **Silent.** PF04 monitors `‖λⁿ⁺¹−λⁿ‖`; BF25 A.2.3 monitors `‖uᵏ⁺¹−uᵏ‖`, `‖qᵏ⁺¹−qᵏ‖`, `‖λ̃ᵏ‖`. | Stagnation alone can report success while the AL constraint `du/dy = q` is still violated — the iteration can crawl. We additionally require feasibility `max|du/dy − q| < tol`, which is what the method is actually solving. | Implicit in M2-T2/T5 |

---

## Elliptic solver (M4)

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| NUM-10 | Buoyancy source sign | `rhs = −∂φB_φ − ∂ξB_ξ` | **Own bug, found and fixed.** | The RHS sign was inverted. It survived every earlier test because uniform `c̄` with β = 0 makes `B` constant and the source identically zero — so the single-fluid, flat-interface and flux gates all ran with `rhs = 0` and could not see it. It only surfaced on an inclined two-fluid steady state, where the solution converged to an O(1)-wrong answer that did **not** improve under refinement. A dedicated test now asserts the source is non-zero in the M4-T2 configuration, so that gate genuinely exercises the sign. | **Yes — `test_m4_t2_buoyancy_source_sign_is_exercised`** |
| NUM-11 | Discretised form | Plain divergence `∂φ(X) + ∂ξ(Y) = 0` | **Own derivation.** BF25 (2.16) is written as `∇a·[S+b]`, which is not a plain divergence and is awkward to discretise. | The pressure-elimination in `docs/derivation.md` produces, en route, an exactly equivalent plain-divergence form with `a_φ = 1/(2r_aH³𝓘₁)`, `a_ξ = r_a/(2H³𝓘₁)`. Equivalence asserted symbolically. Gives a symmetric negative-definite 5-point operator and a single direct sparse solve. | Asserted symbolically |
| BENCH-01 | PF04 (38) | **Re-derived, then validated against PF04's own figures** | **Source unreadable** (text layer scrambled, no renderer), so derived rather than transcribed. | Derived from pressure continuity across a sharp interface with fully-developed 1-D flow on each side; matches the surviving fragments once PF04's jump notation `[·]²₁` is recognised, including the sign of `b cos β`. Identification `1/𝓘₁,k ↔ χ_k(1) + τ_k,Y`. **Now validated on NUMBERS, not just structure:** using the parameter set in PF04's own Fig. 3–5 captions and inverting PF04's 2DGA closure (8) for χ, the derived formula gives \|g(0)\| = **0.411** for β = π/2 against the **±0.42** that Fig. 5's convergence panel shows — ~2%, the precision of reading a printed plot — and exactly **0** for β = 0, matching PF04's statement that the Fig. 3 steady state is "perpendicular to the z-axis". | **Yes — `test_bench01_derived_pf04_38_reproduces_the_published_figures`** |
| BENCH-02 | PF04 (40)–(43) | **Transcribed, implemented, and M4-T3 RESOLVED** | Unblocked by user-supplied page images of PF04 pp. 570–571. | Implemented in `d2dga/gapscale/twodga.py` with the 2DGA closure needed to evaluate them. **Verified:** (40) reduces exactly to (38) at `e = 0` and gives \|g(0)\| = 0.411 against Fig. 5's ±0.42; (42)/(43) both vanish at `z = 0`, so Φ is continuous and the interface is the streamline Φ = 0; both satisfy `∂Φ/∂z = 0` at `z = ±L`, matching B02 (69)–(70); `α_k` real positive, `P > 0`; the O(e) term overtakes the leading one by `e = 0.2`. **Independent confirmation of P:** deriving the O(e) far field from scratch (`Φ_far = e[∂Q/∂χ·τ_Y + ∂Q/∂H − 1]·sin πφ/π`) reproduces PF04's `P(χ,τ_Y,m)` to **ratio 1.000000** for both fluids — so the transcription and the interpretation `Φ = Ψ − Ψ*` are both right. | **Yes — 15 tests** |
| BENCH-03b | M4-T3 resolution | Far field to PF04's own truncation; front converges | — | Two things were wrong, found in order. **CONV-09** (below) was the substantive one — it produced a factor-≈2 mismatch that did **not** converge under refinement. After fixing it, the residual splits cleanly: **far field 1.10% at e = 0.02 and 2.70% at e = 0.05**, a ratio of 2.45 against an e-ratio of 2.5, and it **plateaus under mesh refinement** (0.0267 → 0.0272 → 0.0269). Scaling linearly in `e` and refusing to improve with the mesh is the signature of the **formula's own O(e²) truncation**, not a solver error — a solver bug does not know about `e`. The **frontal** residual does converge with the mesh (0.204 → 0.187 → 0.153 as amp/Δξ goes 9 → 14 → 23), the same staircase limitation as NUM-12, and it bites harder here because the O(e) tilt is small. | **Yes — linearity-in-e test discriminates formula truncation from solver error** |
| BENCH-05 | Why the β = π/2 O(e) test was invalid | Use β = 0 for (40)–(43) | **Validity condition stated in ZF22's summary of PF04**, not in the fragments I had. | ZF22 §4.2: PF04's perturbation holds "when the annulus is mildly eccentric, `e ≪ 1`, **and when the concentric steady-state profile is approximately perpendicular to the annulus axis**". At β = π/2 that profile is tilted ±0.41 — nowhere near perpendicular — so (40)–(43) simply do not apply there. My first O(e) comparison used β = π/2 and was therefore testing the formula outside its own regime. | Corrected; the discrepancy persists at β = 0, so this was not the whole story |
| NUM-12 | M4-T2 domain | Short annulus, `Z ≈ 8` | **Own test-design error, corrected.** | First attempt used the full 196 m K-GEP-1 domain, where `Z ≈ 564` and `Δξ = 0.35–2.8` while the entire steady interface tilt is 0.46 — **sub-cell**, so the benchmark was unresolved and its error did not converge. PF04 works in a short moving-frame window for exactly this reason. | Amplitude/Δξ now 8–180 |

| NUM-13 | Static cells (`𝓘₁ → 0`) | Mobility floored at `1e-12`, count reported as `n_static_cells` | **Silent.** | For a yield-stress fluid `𝓘₁ → 0` where unyielded, so `a = 1/(2r_aH³𝓘₁) → ∞` — the correct limit (it drives `∇Ψ → 0`) but not representable, and `𝓘₂/𝓘₁` becomes 0/0. Flooring is a **regularisation**, and PF04 §5 is explicit that regularisation gives an *overly optimistic* picture of mud removal because nominally static fluid creeps, with error accumulating over a cementing job's timescale. True unyielded-zone capture needs PF04 §3.1's AL at the 2-D level. For Newtonian runs `𝓘₁ > 0` always and this never triggers — a non-zero count on a Newtonian run signals a bug. | Counter exposed; not yet exercised |
| NUM-14 | Tabulated closures for inclined wells | **Refused** (`NotImplementedError`) | **Own limitation, made explicit.** | The gap-scale buoyancy `G̃_b` is a 2-vector in general. For β = 0 it is purely axial, so one scalar `gb` axis suffices — this is K-GEP-1. For β ≠ 0 the table needs an azimuthal buoyancy axis plus the angle between `ū` and `G̃_b`. Rather than silently return a wrong closure, the constructor refuses. | n/a — refuses |
| BENCH-03 | Eccentric analytic benchmark | Derived far-field profile `w̄ = H²/∫₀¹H³dφ` | **Own derivation**, replacing the unrecoverable PF04 (40)–(43). | With `∂/∂ξ = 0` the elliptic equation gives `w̄ = Q H²𝓘₁/∫H³𝓘₁dφ`; for a pure fluid `𝓘₁` cancels, leaving a profile **independent of viscosity** — a real prediction, not a restatement. The solver reproduces it to **machine precision (1e-12)** for e ∈ [0.01, 0.8] and m ∈ {0.2, 5.0}, and the viscosity-independence is asserted separately at a 25× consistency ratio. This covers M4-T3's eccentric content with something derived rather than transcribed. | **Yes — 10 parametrised cases** |

---

## Transport (M5)

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| NUM-15 | Mesh spacings in the buoyancy flux | `Δξ` on a φ-face, `Δφ` on a ξ-face — **restored** | **BCF25 (30), (31), (34), (35) omit them.** | In BCF25's own update (14) everything is multiplied by the single factor `λ = Δt/(ΔφΔξ)`. The advective terms enter through *differences* of Ψ, which already carry a spacing (`Ψ_{i,j} − Ψ_{i,j+1} ≈ ∂ξΨ·Δξ`); the buoyancy terms as printed carry none, so they are short by `Δξ` on a φ-face and `Δφ` on a ξ-face. Taken literally the buoyancy flux would scale like `1/Δξ` — grid-dependent, and the scheme inconsistent. The omission propagates to the LLF coefficients (34)/(35) and hence to the CFL condition (44); it is restored in all four places *consistently*, so BCF25's monotonicity argument is unchanged. | **Yes — `test_num15_buoyancy_flux_is_grid_independent` (3 meshes, rtol 1e-10) and `test_num15_buoyancy_flux_matches_the_closed_form`** |
| NUM-16 | Sign of the azimuthal LLF coefficients | **BCF25 (11)/(14) followed; (18)/(20) as printed rejected** | **BCF25 is internally inconsistent here.** | (18) and (20) print the Ψ-differences in `a_E`, `a_W` with the opposite sign to what (11) and (14) imply. The axial pair (17)/(19) *is* consistent with (12)/(14), which pins the convention. The two readings are not both defensible: with `v̄ > 0` we have `∂ξΨ = −2Hv̄ < 0`, and (11)/(14) then give `a_W > 0, a_E = 0` (upwind from upstream) while (18)/(20) give `a_W = 0, a_E > 0` — upwinding from **downstream**, which is unstable. | **Yes — `test_num16_azimuthal_upwind_direction`** |
| NUM-17 | LLF wavespeeds `∂q₀/∂c̄`, `∂𝓘₃/∂c̄` | **Analytic** for Newtonian and 2DGA; central FD × (1 + 1e-6) for tabulated | BCF25 (32)–(37) require them but does not say how they are obtained | Monotonicity needs the wavespeed to bound the true slope **from above** at the neighbouring cell values. A difference quotient can sit O(h²) *below* it, which turns the discrete maximum principle into an approximate one. Both Newtonian closures are rational in `c̄`, so exact differentiation is free; the tabulated path keeps a small explicit safety factor instead. BCF25 (37) makes the same "bound above" point for the multi-fluid case. | ⚠️ **Corrected (B-3).** This cell previously read "endpoint-only evaluation is *sufficient*, not an approximation … so no interval maximum is needed". That is the opposite of **NUM-26**, which measures a maximum-principle violation of −0.0089 under endpoint-only wavespeeds, and it survived here after NUM-26 was fixed. Endpoint-only is **not** sufficient. The interval maximum is required; `wavespeed="interval"` is the default and `test_b3_interval_wavespeed_is_the_default` pins it. |
| NUM-26 | LLF wavespeed | **Maximum of \|f′\| over the whole closed interval between the two face states — computed exactly from the closures' interior extrema.** `wavespeed="endpoints"` reproduces BCF25 as printed and is kept only to demonstrate the failure. | **BCF25 (32)–(35) evaluate the slopes at the two cell values only, which is not sufficient.** | The coefficient then depends on the neighbour's own value, and where `\|d𝓘₃/dc̄\|` is *falling* it **decreases** as that neighbour's concentration rises. Monotonicity needs `d/dc̄_S[α(c̄_S,c̄_C)(c̄_S−c̄_C)] ≥ 0`; with an endpoint-only `α` the `(∂α/∂c̄_S)` term can overwhelm `α`. **Measured**, at β = π/4, b = 20, e = 0.5: the discrete Jacobian entry for the upstream neighbour is **−0.070** and the concentration reaches **−0.0089 within two steps** — an O(10⁻²) violation, not roundoff. ⚠️ It passed every M5 gate: M5-T2's parameters happened not to reach the falling branch of `\|𝓘₃′\|` with a large enough state jump, so the coupled loop is what exposed it. The textbook LLF/Rusanov interval maximum fixes it **unconditionally** — it bounds the slope at both endpoints *and* cannot decrease as the interval widens, so the product is non-negative for either ordering of the two states. | **Yes — `test_num26_endpoint_wavespeeds_break_monotonicity` runs both variants on the state that exposed it** |
| NUM-29 | Flux at the **inflow** face | **Physical upwind flux** — pure upwinding, the buoyancy term at the boundary state, no artificial dissipation. `inlet_flux="llf"` restores the ghost-cell form and is kept only to show the size of the error. | **Own bug, found by an invariant that should have been checked from the start.** | Applying the interior LLF flux at `ξ = 0` through a ghost cell puts `−α(c_R − c_L)` on a face with no second cell to redistribute to. In the interior that term is a conservative diffusion; at a Dirichlet inflow it is a pure **source**, sized by the LLF wavespeed rather than by anything physical. Decomposing the K-GEP-1 inflow face at `t = 0`: volumetric flow **1.0000** (= Q, so the elliptic BC was right), advective **0.5000**, buoyancy **0.0000**, **LLF dissipation 110.1255** — the annulus was being filled with cement at **110× the rate it was pumped**. The symptom was visible and I nearly walked past it: `η = 0.034` after `0.009` of a volume. ⚠️ The published benchmarks hid it: their `α` is ~2.4 rather than 110, so the ZF22 comparison still came out within 2.5% — **the defect scales with the buoyancy wavespeed, which on this fluid pair is 10³× the mean flow (FLU-06)**. The fixed face delivers exactly `Q·c̄_in`. | **Yes — `test_num29_inflow_face_delivers_exactly_the_pumped_flux` asserts the flux to 1e-12 and the invariant "what is in the annulus cannot exceed what was pumped", and shows the ghost-cell form breaking both** |
| NUM-27 | Picard: seed and tolerance | **Warm-started from the previous step's Ψ; `picard_tol` default 1e-8 (was 1e-10).** | BF25 A.2.3 suggests continuation but gives no tolerance. | From a cold start the Picard residual on the K-GEP-1 HB pair falls only *linearly* — 40 iterations reach 1.8e-5. Seeded with the previous step's Ψ it converges in **5–6**, which over ~10⁵ steps is the difference between hours and days. The tolerance was also tighter than the time discretisation warrants: the transport step is **first order in Δt**, so driving the elliptic residual to near machine precision every step buys nothing. | Exercised by every HB run; M6-T4 measures what the Δt-level error actually is |
| NUM-28 | Tabulated c-derivatives | **Bound read from the table's own piecewise-linear structure, not finite-differenced.** | n/a — implementation | The LLF interval maxima (NUM-26) need `\|dq₀/dc̄\|` and `\|d𝓘₃/dc̄\|` at 29 probe nodes on every face. Finite-differencing them cost **eight** quantity-interpolations apiece, because `q0_I3` interpolates all four closures and a central difference takes two calls. Since the table is linear in `c̄` its derivative is the segment slope; storing `max(\|slope_{k−1}\|, \|slope_k\|)` at node `k` and interpolating *that* still bounds the true slope everywhere — inside segment `k` both endpoint values are ≥ `\|slope_k\|`, so any convex combination is too — and a bound is exactly what monotonicity needs. **Measured: transport 277 → 65 ms per step, whole step 310 → 105 ms.** ⚠️ The split was measured, not guessed: the cost was neither the sparse solve (4 ms) nor the table lookups (6 ms). | Covered by the M5/M6 monotonicity gates, which is what the bound exists to buy |
| NUM-19 | φ-boundary flux | Set to **exactly zero** at φ = 0 and φ = 1 | B02 §3.2 symmetry planes | The advective part is already exactly zero there (Ψ is constant along both boundaries), which is asserted separately; only the buoyancy part needs the explicit zeroing. This is what makes M5-T1's telescoping exact — the azimuthal flux differences cancel to the two symmetry planes and vanish. | **Yes — `test_m5_t1_azimuthal_flux_vanishes_on_the_symmetry_planes`** |
| NUM-20 | Outflow at ξ = Z | Zero-gradient ghost cell | Silent in all sources | Advection-dominated outflow; the ghost equals the last interior cell, so the LLF dissipation on that face is identically zero and the face carries the pure flux. Uniform states are still preserved exactly, which is the property that would break first if the ghost were wrong. | **Yes — `test_uniform_states_are_preserved_exactly`** |

### Bugs found in the published scheme and in this implementation

| ID | What | Where |
|---|---|---|
| NUM-18 | `areal_flux` (PF04 (8)) evaluated `χ^(m+1)/G²`, which is **0/0 for a yield-stress-free fluid**: `G = χ` there, and squaring anything below 1e-154 underflows to zero. The root finder brackets from 1e-300, so this fired on **every Newtonian 2DGA solve** and raised out of `brentq`. Rewritten as `χ^m · (χ/G) · ((χ+a)/G)` — algebraically identical, both ratios bounded. **Own bug**, exposed by M5-T3, which is the first test to drive the 2DGA path with `τ_Y = 0`. | `gapscale/twodga.py` |

---

## Measured caliper vs synthetic wall — what the real hole costs

First end-to-end run of the `--wall caliper` path (16 × 80, K-GEP-1 fluids,
`ŵ₀ = 0.2 m/s`).  It works — axial flux `2Q` to 1e-9, `c̄ ∈ [0, 1]`, no static
cells — and it is a much harder problem than the synthetic wall:

| | synthetic (A = 1.5 in, L = 20 m) | **measured caliper** |
|---|---|---|
| `Z` | 564.1 | 524.8 |
| `H` range | 0.263 – 1.758 (×6.7) | **0.054 – 3.747 (×70)** |
| `r_a` range | 0.913 – 1.085 (19%) | **0.815 – 1.583 (94%)** |
| `e` range | 0.21 – 0.54 | **0.065 – 0.814** |
| `δ/π` max | 0.083 | **0.168** |
| `\|dr_o/dξ\|` max | 0.012 | **0.163** |
| `b` | 27.9 | 37.4 |
| CFL-limited `Δt` | 5.8e-3 | **7.7e-4** |

Three consequences, all of which have to be said before any caliper number is
quoted:

* **CONV-04 and CONV-10 are load-bearing here and only here.** `r_a` varies by
  94% along the real hole against 19% on the synthetic one and 0% in every
  published case. The two `r_a` ambiguities in BF25/BCF25 — which cancel in the
  elliptic source and survive in the transport flux — were derived rather than
  pattern-matched precisely because this geometry can tell them apart.
* **The validity envelope is strained.** `δ/π = 0.168` and a wall slope of
  0.163 are both an order of magnitude beyond the published cases, and the
  Hele-Shaw reduction assumes both are small. The caliper result is a
  *prediction under a stated assumption*, not a converged answer, and M0's
  validity-envelope plot (`output/caliper_validity_envelope.png`) is the figure
  that says where.
* **It costs ~7× the synthetic run.** `Δt` falls by 7.5× because the buoyancy
  wavespeed scales with `H³` and `H_max³` is 52 against 5.4. A full 1.2-volume
  caliper run is ~16 h at 16 × 80 — beyond a single container window even with
  checkpointing, so it needs either a coarser mesh or several resumes.

---

## Coupled loop (M6)

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| NUM-21 | Breakthrough criterion | **Reported as a sweep over outlet thresholds 0.01 / 0.1 / 0.5, not a single number** | ZF22 §6: "the time at which the displacing fluid first exits the annulus" | That wording is exact only for a **sharp** front, and D2DGA fronts are not sharp by construction. BF25 §3.1's *spike* regime puts a vanishingly thin tip ahead of the main shock which travels at the Poiseuille centreline speed **1.5 whatever the buoyancy number** — BF25 §3.2 says so explicitly ("it maintains a constant value of 1.5"). So a small threshold measures the spike and a large one the shock behind it, and for the weakly buoyant ZF22 cases the two differ by a factor of 1.5. Picking one threshold would amount to choosing the number that flatters the comparison. The 1-D kinematic-wave shock time is printed alongside, which brackets the answer from theory rather than from a tuned constant. | **Yes — `scripts/zf22_table3.py` reports all three plus the 1-D prediction** |
| NUM-22 | Elliptic/transport coupling | Ψⁿ from cⁿ, then one explicit transport step | BCF25 §III assumes it; never states the order | BF25 (2.16) is a **constraint**, not an evolution equation — Ψ is determined instantaneously by `c̄` and `Q` — so the only time discretisation in the model is the explicit Euler step in the transport equation. Two consequences, both load-bearing: the LLF bound BCF25 (44) is evaluated on `Ψⁿ` and `c̄ⁿ`, both known before the step, so M5's maximum principle carries over unchanged; and the Ψ/`c̄` lag over the step is O(Δt), which is the error M5 could not see and M6-T4 measures. | **Yes — M6-T2 (conservation and the maximum principle with Ψ recomputed every step, including b < 0) and M6-T4 (CFL 0.5 vs 0.25)** |
| NUM-23 | Elliptic assembly | Vectorised; row-by-row version retained as `assemble_reference` | n/a — implementation | The coupled loop reassembles the matrix at every one of 10³–10⁵ steps, and a Python loop over `n_phi × n_xi` cells dominated the runtime (16× slower). The two are asserted **bit for bit identical**, not merely close — including the floating-point *association* in the uniform-inflow right-hand side, which a first attempt got wrong by a harmless-looking 7e-15. A drift that size would accumulate over a full run and be indistinguishable from physics. | **Yes — `test_vectorised_assembly_matches_the_reference_bit_for_bit`** |

### Benchmarks

| ID | What | Result |
|---|---|---|
| BENCH-06 | **BF25 §3.1 kinematic-wave structure** (`tests/benchmarks/kinematic_wave.py`) | The planar reduction BF25 (3.7) is a scalar conservation law whose Riemann solution is the **upper concave envelope** of `f(c̄) = q₀ + b𝓘₃` on [0,1]. Implemented as a monotone-chain hull and it reproduces all four of BF25's regimes from the flux function alone. Quantitatively: at `b = 0` the dispersive→shock transition sits at **m = 1.5 exactly**, which BF25 §3.2 attributes to Lajeunesse et al. (1999) as `M₃ᵐⁱⁿ`; for `b > 0` the main shock speed falls to 1 (1.107 → 1.037 → 1.013 as `b_BF25` goes 4.5 → 45 → 447) and the spike width collapses like `2√m/b`, matching BF25's "buoyancy completely suppresses the spike regime as b increases from 100 to 1000". ⚠️ **One discrepancy, reported rather than hidden:** BF25 §3.2's text says the shock speed "is equal to 1.5" for `1.5 < m < 2.25`. It is not exactly 1.5 — we get 1.500 at m = 1.55, 1.508 at m = 2.0, 1.517 at m = 2.25, rising smoothly with no kink at 2.25. But BF25's **own figure 12(a)** carries a colour bar spanning **1.500–1.520** over exactly this range, so the figure and our numbers agree and it is the sentence that is loose. |
| BENCH-07 | ZF22 Table 3, 1-D part | The 1-D shock time `1/w_shock` tracks ZF22's printed `t_br` across the ten cases: **0.98 vs 0.99 (case 4), 0.95 vs 0.95 (case 2), 0.93 vs 0.93 (case 6), 0.39 vs 0.44 (case 1, the only adverse-buoyancy case)**. The weakly buoyant cases 3/7/8 sit between the 1-D shock time and the spike tip time, which is the NUM-21 ambiguity, not a model error. |

---

## M6-T1 result: ZF22 Table 3

`output/zf22_table3.md`, mesh 20 x 200, CFL 0.5.  Displacement efficiency is
the cleanest comparison — it is threshold-free, unlike `t_br` (NUM-21).

| case | b (ZF22) | η_E ours | η_E ZF22 | error |
|---|---|---|---|---|
| 1 | **−50** | 0.366 | 0.66 | **−45%** ⚠️ |
| 2 | 100 | 0.952 | 0.95 | +0.2% |
| 3 | 10 | 0.897 | 0.92 | −2.5% |
| 4 | 1000 | 0.983 | 1.00 | −1.7% |
| 5 | 100 | 0.957 | 0.97 | −1.3% |
| 6 | 100 | 0.940 | 0.93 | +1.1% |
| 7 | 10 | 0.898 | 0.90 | −0.2% |
| 8 | 10 | 0.858 | 0.84 | +2.1% |
| 9 | 100 | 0.959 | 0.97 | −1.1% |
| 10 | 100 | 0.959 | 0.97 | −1.1% |

**Nine of ten within 2.5%.**  `t_br` at the 0.5 threshold agrees to within 0.08
on all nine, most within 0.03.  Cases 9 and 10 differ only in `Re`, which the
D2DGA model does not contain — both we and ZF22 report identical values for
them, which is a consistency check on the comparison itself.

### Case 1 — the one failure, and what is known about it

| ID | Finding |
|---|---|
| BENCH-09 | **Case 1 is not reproduced: `t_br` = 0.054 against ZF22's 0.44, `η_E` = 0.366 against 0.66.** *(B-5: this row previously read 0.059 / 0.375, from a run superseded by the NUM-26 and NUM-29 fixes. Re-run in full 2026-09-18; `output/zf22_table3.md` is the single record and all ten cases reproduce it exactly.)* ⚠️ **A-5 supersedes claim (i) below:** mesh convergence holds for `t_br` and NOT for `η_E`. Azimuthal refinement at fixed `n_xi = 200` gives `η_E` = 0.3657 → 0.3939 → 0.4773 for `n_phi` = 20 → 40 → 80 (increments GROWING); axial refinement at fixed `n_phi = 20` gives 0.3657 → 0.3508 → 0.3340 (increments flat, and in the opposite direction). Neither direction converges; the 20×200 → 40×400 diagonal BENCH-09 used is where the two errors cancel. See `docs/remediation_log.md` A-5 and `BLOCKED.md` BLK-6. It is the only case with `b < 0` (displacing fluid *lighter*, so heavy fluid sits above light — gravitationally unstable) and the highest eccentricity (`e = 0.8`). What has been established: **(i) it is mesh-converged** — `t_br` moves from 0.0534 to 0.0530 between 20×200 and 40×400 while `Δφ, Δξ` halve, so it is not a resolution artefact and not an unregularised instability whose growth rate rises with wavenumber; **(ii) the 1-D theory agrees with ZF22** — BF25 §3.1's leading wave speed for these parameters gives `t_br = 0.391` against ZF22's 0.44, so the gap-scale closures are not the problem; **(iii) it is the 2-D coupling that diverges** — the buoyancy drives a counter-current cell reaching `w̄ ∈ [−10, +17]` against a mean of 1, and the wide-side channel breaks through at 17× the mean; **(iv) it is dominated by buoyancy, not eccentricity** — at `b = 0, e = 0.8` the peak is only `w̄ = 1.65` (exactly the analytic wide-side channel factor `H²_wide/∫H³dφ`) and there is no early breakthrough, while at `e = 0`, `b = −22.4` the peak is already 5.6; **(v) it is strongly sensitive to the bottom-hole condition** — B02 (70) gives `t_br` = 0.059 and B02's uniform-inflow alternative gives no breakthrough at all within 0.25 Z. That last point contradicts NUM-04's earlier finding that the inflow choice is local to within 1.3 m, which was established on a *stable* configuration; with adverse buoyancy the counter-current cell reaches the inlet and the choice becomes global. ⚠️ **One hypothesis was tested and refuted**: that buoyant exchange across the inlet face was pumping in extra displacing fluid. Suppressing it changes `η_E` only from 0.375 to 0.347, so it is not the cause; the excess enters through the *advective* backflow the zero-gradient condition permits. **Second hypothesis tested and refuted (2026-09-17): the bottom-hole condition.** NUM-04b showed B02 (70) admits backflow, so the obvious next guess was that case 1's early breakthrough is a boundary artefact. It is not. Re-running case 1 with the uniform-inflow condition — which forbids backflow and closes the volume balance exactly — moves it **further from ZF22, not closer**: `η_E = 0.1145` and no `c̄ = 0.5` breakthrough at all within 1.2 volumes, against B02 (70)'s 0.366 and ZF22's 0.66. A control at case 3 (`b > 0`, stable) gives **byte-identical results for both conditions**, which confirms the boundary only matters when the buoyancy is adverse, and that it is not the discrepancy. **So ruled out for case 1: buoyant exchange across the inlet face, and the bottom-hole condition. Still not explained.** ZF22 itself singles this case out ("we verify again via the poor efficiency of case 1 that `b < 0` should be strictly avoided"), and K-GEP-1 has `b > 0`, so this does not affect the well being modelled — but it is an open discrepancy and is recorded as one rather than argued away. |

---

## Post-processing (M7)

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| NUM-24 | Azimuthal averaging weight | **Volume weight `H·r_a`**, not cell count | ZF22/ZF23 do not say | At `e = 0.8` the gap varies by a factor of 9 across `φ`, so a plain mean would count the narrow side — where the mud actually survives — as heavily as the wide side, and would over-report efficiency. The weighted average is also the one consistent with `η_E`, which is a volume ratio by definition. | **Yes — `test_efficiency_is_volume_weighted_not_cell_counted`, `test_m7_axial_profile_is_volume_weighted`** |
| NUM-25 | `σ_{w−r}` binning | **Reported with its bin count; the area measures `\|w̄_r±\|` used for any actual conclusion** | ZF23 (3.3): "segmenting c̄ and summing over a finite number of discrete c̄ values" — count not given | `σ_{w−r}` is **not a converged quantity**. `w_f → 0` as `c̄ → 1` for every `m` (because `q₀'(1) = 0`), so `w_r → −1` and the last bins dominate the sum. Measured over a 32× range of bin counts (25→800) it moves by **14% and non-monotonically** — the successive differences change sign, so it is not a sequence approaching a limit. The practical consequence: **no `σ_{w−r}` comparison against ZF23 can be read as tighter than ~15%**, and BENCH-08 states its agreement at that level and no better. ZF23 introduced (3.4)–(3.5) precisely because "the standard deviations are influenced by large deviations", and those **do** converge. Flagged so that no later reader treats a `σ_{w−r}` comparison as tighter than it is. | **Yes — `test_m7_sigma_minus_is_bin_dependent_and_the_area_measure_is_not` asserts both halves** |

### Benchmarks

| ID | What | Result |
|---|---|---|
| BENCH-08 | **ZF23 Table 3, D2DGA columns** (both cases are concentric, so the flow is planar and BF25 §3.1 applies exactly) | **EXP 48** (`e=0, b_ZF=30, m=0.8`): `σ_{w+r}` **0.122 vs 0.1369** (−11%), `σ_{w−r}` **0.601 vs 0.5493** (+9%). **EXP 96** (`e=0, b_ZF=750, m=0.2`): `σ_{w+r}` **0.021 vs 0.0368**. ⚠️ The EXP 96 gap is the dispersive **spike**: at `b_BF25 = 335` it is only `2√m/b = 0.0027` wide in concentration, so the exact theory puts almost all of it inside a single bin while a finite mesh smears it across several — ZF23's own figure 7 caption says "a finer mesh would better represent the spike". Both agree on every ZF23 (3.6) threshold: EXP 48 dispersive, EXP 96 not. |

---

## Fluids (K-GEP-1)

Source, user-selected 2026-09-16: **Table 1 of "Numerical Analysis of Cement
Placement Into Drilling Fluid in Oilwell Applications", *Materials* 2025, 18,
3098.**  Properties taken verbatim; nothing else from that paper is used.

| | cement slurry (displacing, fluid 2) | drilling fluid (displaced, fluid 1) |
|---|---|---|
| density | 1200 kg/m³ | 998 kg/m³ |
| model | Herschel–Bulkley | Newtonian |
| viscosity | — | 1×10⁻³ Pa·s |
| consistency `κ` | 0.6 Pa·sⁿ | — |
| flow index `n` | 0.4 | — |
| yield stress `τ_Y` | 1.4 Pa | — |

Resulting dimensionless groups on K-GEP-1 (`r̂_a* = 110.6 mm`, `δ* = 0.193`,
`Z = 564`):

| `ŵ₀` | `m` | `B` | `b` | `Fr*` |
|---|---|---|---|---|
| 0.05 m/s | 0.00278 | 31.5 | 29.3 | 0.0831 |
| 0.20 m/s | 0.00638 | 11.9 | 27.9 | 0.0852 |
| 0.50 m/s | 0.01105 | 6.29 | 26.1 | 0.0881 |

| ID | Issue | Why it has to travel with every result |
|---|---|---|
| FLU-01 | **The source's "drilling fluid" is WATER** — 998 kg/m³, 1 mPa·s, Newtonian. | A real drilling mud is 1100–1600 kg/m³ and carries a yield stress. The consequence is `m ≈ 0.003–0.011`: the displacing fluid comes out **100–360× more viscous** than the displaced one, which is an unusually *favourable* viscosity ratio. Any displacement efficiency computed from this pair is **optimistic**, and the number should not be quoted as a K-GEP-1 prediction without saying so. |
| FLU-02 | Its cement is **denser** than its "mud" (1200 vs 998), so `b > 0`. | Favourable and stabilising, landing at `b = 26–29` — comparable to ZF22's strongly buoyant cases 2/5/9, which displace near-piston. The resulting 1-D flux `q₀ + b𝓘₃` reaches **−25** in mid-range, far below the chord, so the entropy solution is a shock at the mean speed. Good displacement is the *expected* answer here, which is exactly why FLU-01 matters. |
| FLU-03 | Its flow is **downward** ("the cement slurry inlet velocity, which is downward"). | Ours — and B02/PF04/ZF22/BF25/BCF25 without exception — is *upward* annular displacement. Only the properties cross over; the geometry, flow direction and boundary conditions do not. |
| FLU-04 | The paper's 0.5 / 0.2 / 0.05 m/s are used as the **annular mean velocity** `ŵ₀`. | They are quoted as inlet velocities in that paper's own, much smaller annulus. Treating them as K-GEP-1's annular mean velocity is a *reading*, not a measurement. The pump schedule for K-GEP-1 is still outstanding. |
| FLU-06 | **The flow is buoyancy-dominated by ~3 orders of magnitude, and that follows directly from FLU-01.** | The elliptic buoyancy source scales as `b·𝓘₁`: `b` measures buoyancy against the viscous stress scale, and `𝓘₁` measures how mobile the fluid is at that scale. With water as the displaced fluid `𝓘₁(c̄=0) = 54` (against ~0.75 for ZF22's cases), so **`b·𝓘₁ = 1507`** — any *azimuthal* non-uniformity of the front is amplified into velocities ~10³ times the mean. Measured: a perfectly flat front stays well behaved (`w̄ ∈ [0.1, 3.7]`, `v̄ ≈ 0`, axial flux exactly 2Q, because `∂_φb_s = 0` when `c̄` does not vary with `φ`); once eccentricity tilts the front, `\|ū\|` runs past 370 within the first timesteps and off the closure table's velocity axis. **This is the model correctly reporting the consequence of its inputs, not a solver failure**: with a water-thin displaced fluid and a 202 kg/m³ favourable density difference, gap-scale slumping simply overwhelms a 0.2 m/s pump rate. Two practical consequences: the displacement is **not pump-rate controlled**, so comparing the paper's three velocities answers nothing; and the LLF timestep is ~10³ times tighter, which makes a full 196 m run very expensive. ⚠️ It is also far outside the regime any of the source papers tested — theirs run to `b ≤ 1000` with mobility contrasts of ~5, not 10³. **Raised with the user and re-affirmed on 2026-09-17: the mud is to be treated as water-like, per the source paper.** Runs therefore proceed with these properties and every K-GEP-1 number carries this caveat — it describes a water-displacement, not a mud-displacement, and the efficiency it reports is an upper bound on what a real mud would give. | Quantified above; the closure table's M3-T3 range guard is what caught it, by name, rather than silently extrapolating |
| FLU-05 | The pair is **Herschel–Bulkley with large `B`**, so it runs the M3 tabulated-closure path and the Picard elliptic, not the fast Newtonian one. | NUM-13's mobility-floor regularisation becomes live, and PF04 §5 warns that regularisation flatters mud removal. Measured at `ŵ₀ = 0.2 m/s` the cement is **fully yielded** (unyielded fraction 0 at unit mean velocity), so the regularisation should not bite — but `n_static_cells` must be checked on every run and reported, not assumed zero. |
| FLU-07 | **The mud may now be Herschel-Bulkley, and the θ = 0 closure assumption (B-1) is governed by `m`, not by the mud's rheology.** Added 2026-09-19 at the user's request (UI gap G-1); the default is still FLU-01's water, bit-identical. | Holding the mud **strictly Newtonian** and moving only its viscosity against the unchanged K-GEP-1 cement, the B-1 angle sensitivity runs `0.53% → 2.0% → 11.4% → 35.7% → 93.1% → 168.6%` at `κ̂₁ = 1, 2, 5, 10, 20, 50 mPa·s` (`m = 0.0063 … 0.317`); a pair with **both** fluids Newtonian measures exactly `0.00%`. `tables.py` explained the benign K-GEP-1 figure by "the displaced fluid is Newtonian" and **`AUDIT_REPORT.md` B-1 repeats that wording — it is the wrong attribution**, corrected in the docstring and logged as REM-11 (the audit report itself must not be edited). What makes the shipped pair benign is `m ≈ 0.006`: the mud is ~160× thinner, so the cement's yielded structure — the only θ-sensitive part — barely feels the mud layer. **Consequence: this hazard predates G-1 and reverting G-1 would not close it.** A Newtonian mud at an ordinary 5 mPa·s, expressible by editing one field that already existed, is already at 11%. So the three fields stay and the on-screen disclaimer is keyed to the *measured* value rather than to whether the mud is Newtonian. **⚠️ And the figures above hold only for a probe axis capped at `|ū| = 10`.** The sensitivity grows with `|ū|`, and the production axis reaches 3000 (it must — FLU-06 drives `|ū|` past 370). Rebuilt on the production axes the **shipped pair measures 9.8% — SIGNIFICANT, not benign** — peaking at `|ū| = 3000`. It had never been measured there, because the production runs *loaded* a cached table and a loaded table reports `not measured`. No computed number moves, but `AUDIT_REPORT.md` B-1's "benign for the K-GEP-1 pair" is **not supported at the resolution the runs use**, and 9.8% must travel with every K-GEP-1 figure. The peak sits at large `|ū|`, which is where the front is most tilted — the two errors are correlated, not independent. Read it as an upper bound on the angle error (θ = 0 vs θ = 90°), not as the error in `η_E`. The fix is the fifth table axis (NUM-14). Cost note: the same production build on a Herschel-Bulkley mud took 954 s and left 12 of 935 gap solves unconverged, against 0 for water. | **Yes — M0-C9/C10 lock the angle-exact Newtonian pair and the monotone m-sweep, with one-sided thresholds far from the measured values** |

---

## Measured caliper, K-GEP-1 (`data/K-GEP-01_2024-03-30.las`)

LAS 2.0, `WRAP=YES`, curve `BOREHOLE_DIAMETER` in INCH, `DEPT` in m below ground
level, step 0.01 m, NULL −99999. 39 076 raw samples, 199 null.

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| CAL-01 | Gauge hole | **≈10.1 in after bias correction**, or 10.5 in as logged | **CORRECTED.** An earlier entry read "gauge hole confirmed at 10.43 in" — that conflated the cased and open-hole sections and was wrong. | From 5–190 m the tool reads **10.432 in with std 0.007** — far too uniform for rock. That is the **10" casing ID**, not the formation, as the casing programme confirms. Washout begins abruptly at 194–196 m, exactly at the shoe. This turns the cased section into an **in-situ calibration reference**: against a nominal 10.00 in ID the tool carries a **+0.43 in (+4.3%)** bias; against 10.19 in ID, +0.24 in. Open-hole median is 10.51 in as logged, so true gauge is ≈10.07–10.12 in. ⚠️ The bias cannot be pinned without the casing weight/grade that fixes the exact ID. | Bias bracketed over plausible IDs |
| CAL-02 | Bottom-hole artefact | Interval truncated at **386.7 m**; `min_diameter_m = 7.2 in` as a backstop | **Data defect.** | One contiguous 3.2 m run, 386.76–389.95 m, falls to **0.77 in** — the caliper arms closing at TD, not a 2 cm borehole. It is the *only* sub-gauge run in the open hole (320 of 19 596 samples, 1.6%). Samples are **dropped, not clamped**: they carry no geometric information. Truncating also removes the shoe-track region, which is not part of the cemented annulus of interest. | Asserted in `test_caliper_rejects_the_bottom_hole_tool_artefact` |
| CAL-03 | Tool ceiling | Accepted as-is, flagged | **Spec warned of clipping above 24 in; confirmed but negligible.** | Exactly 7 samples (0.04%) sit at 24.009 in, the tool maximum. Those readings are **lower bounds**, so the true washout is at least that wide. Too few to matter; `n_at_ceiling` is reported so it cannot be forgotten. | n/a |
| CAL-04 | Resampling | Block-average diameter onto **0.25 m** | **Silent.** | The raw log is 0.01 m; a 400-cell axial grid over K-GEP-1 is 0.48 m/cell, so averaging is unavoidable. Doing it explicitly at 0.25 m (≈2 samples per computational cell) beats letting the interpolator do it silently at an arbitrary phase. **The scale matters:** median \|dr̂_o/dξ̂\| falls 0.0095 → 0.0030 and the max 2.61 → 0.13 between 0.01 m and 0.48 m averaging. | **Yes — gradient vs averaging scale tabulated over 0.01–5 m** |
| CAL-05 | Depth convention | `ξ̂ = 390.0 − depth` | **Explicit in the LAS.** | LAS depth is m below ground level (`DEPTH_LOGGER 390 m.b.g.l.`); the model's ξ̂ runs up from bottom hole. `total_depth_m` is a required argument, not inferred, because the deepest logged sample (389.95 m) need not be TD. | Asserted in `test_caliper_depth_convention_is_inverted` |
| CAL-06 | **Headline geometric result** | — | — | On the computational grid the measured wall is far more demanding than the synthetic case the model was developed against: `δ/π` median **0.064**, p95 **0.150**, max **0.173** — **97.6% of the well above ZF23's validated 0.038**; \|dr̂_o/dξ̂\| max **0.33 vs the synthetic wall's 0.006 (55×)**; eccentricity reaches **0.858**. 41.5% of the open hole is at gauge, 20.5% washed beyond 12 in. **This is the validity envelope the thesis exists to report, and it says the reduced model is being pushed hard over most of K-GEP-1.** | **Yes — `test_caliper_is_far_rougher_than_the_synthetic_wall`** |
| CONV-09 | **Fluid numbering is reversed between PF04 and BF25** | PF04's ordering used inside `twodga.py`; callers swap | **Direct conflict, both stated verbatim.** PF04 §2.2: "Ω₁ for the **displacing (lower)** fluid 1, and Ω₂ for the **displaced (upper)** fluid 2". BF25 §2.3: "k = 1 for the **displaced** fluid and k = 2 for the **displacing** fluid". | Exactly opposite, so PF04's fluid 1 is BF25's fluid 2. Every index in (40)–(43) — the jump `[·]²₁`, and which `P` and `α` sit on which side of `z = 0` — is in PF04's ordering. PF04's `b = (ρ_upper − ρ_lower)/St*` is also **negative for the favourable case**, opposite to BF25's sign. **Invisible in \|g(0)\| at β = π/2**, where `\|g(0)\| = \|b\|/(π\|J\|)` and both sign flips cancel — which is why the (38) magnitude check passed while the fluids were swapped. It bites only once the two fluids have different properties *and* the two sides of the interface are compared separately, i.e. exactly in (42)/(43), where the two far-field amplitudes differ by ~15%. | **Yes — `test_conv09_fluid_numbering_is_the_one_that_bites`** |

---

## Remediation (2026-09-18) — choices made while resolving `AUDIT_REPORT.md`

One row per choice. Detail and derivations in `docs/remediation_log.md`;
unresolved items in `BLOCKED.md`.

| ID | Choice | Value used | Source status | Justification | Sensitivity tested? |
|---|---|---|---|---|---|
| REM-01 (A-1) | Outflow condition at `ξ = Z` | **Zero-gradient ghost — left unchanged** | **Silent in all six papers.** | The audit read the negative flux there as a defect. It is not: for these flux functions the upper concave envelope of `f = q₀ + b𝓘₃` is the straight chord, so the intermediate concentrations that make `f` negative **never occur in the exact solution** — the flux is manufactured by the first-order smearing. Measured against the exact entropy solution it vanishes at **exactly first order** (rates 1.00, 1.00, 1.01 at `b_ZF22 = 100`; 1.00, 1.00 at 1000; identically zero at 10, where `f ≥ 0` everywhere). Zero-gradient is also the **only** closure that preserves a uniform state exactly — injecting a `c̄ = 0` ghost breaks `test_uniform_states_are_preserved_exactly`. Changed nothing; added the missing observability instead. | **Yes — `test_a1_outlet_import_vanishes_at_first_order` locks the rate (a real defect plateaus); verified to fail under an injected `c̄ = 0` ghost** |
| REM-02 (A-1) | Outlet-import accounting | `TransportSolver.outlet_import`, printed by `scripts/kgep1_run.py` against capacity | n/a — diagnostic | An artefact nobody measures becomes permanent; that is the lesson of A-3. The expectation ("halves when `n_xi` doubles") is printed with the number so a future non-convergent case is visible rather than inferred. | Covered by REM-01's gate |
| REM-03 (A-3) | Closure-table range guard | **Unsuppressible record on the table** (`n_range_points_out`, `worst_excursion`, `range_report()`); the `UserWarning` is kept as well | **Own defect.** | The guard was only a warning, and the production runs' `-W ignore::UserWarning` erased the sole evidence that a result rested on **extrapolated** closures — while FLU-06 credits that guard with catching exactly this. A guard a command-line flag can erase is not a guard. | **Yes — `test_a3_range_record_survives_warning_suppression` runs the query under `simplefilter("ignore")` and asserts the record survives** |
| REM-04 (A-3) | `umag` axis floor | **0.0** (was 0.02) | **Own defect.** | `\|ū\| ≥ 0` always, so anchoring at 0 makes an under-range query impossible. Costs one extra plane (6 % of the build). **Changes nothing numerically**: the gap solve at `umag = 0`, `1e-3` and `0.02` agrees to **six significant figures** on the K-GEP-1 pair, which is why the extrapolation that did occur (315 points of 25 899 552) was harmless and why the *detection* mattered more than the extrapolation. | Measured at three `c̄`, three `H` |
| REM-05 (B-1) | Angle between `ū` and `G̃_b` | **`θ = 0` slice retained; measured and reported per table** | **Own limitation, previously mis-stated as a reduction.** | `G̃_b` being purely axial does **not** collapse the angle — `ū` is still a 2-vector and `θ ≠ 0` whenever the front is tilted. The fix is a fifth axis, which is a new capability. Instead `build()` measures the sensitivity for its own pair (18 solves against 4 743) and `assumption_report()` states it with a **graded** verdict: benign < 5 %, SIGNIFICANT below 50 % (naming the `\|ū\|` where it peaks, so a table sensitive only at its axis extreme is not reported like one that is O(1) wrong in the middle), LARGE above. K-GEP-1's table: 0.0 % at `\|ū\| = 0`, 0.4 % at the median, 9.8 % at `\|ū\| = 3000`, against a run that never exceeds 75. | **Yes — three tests: the O(1) deviations on a yield-stress pair, the build-time report, and `< 1e-6` for a Newtonian pair (the reason no gate caught it)** |
| REM-06 (A-2) | Reporting of `t_br` | **Printed with a half-order discretisation bracket**, not to four bare decimals | **Own claim, corrected.** | Derived: the LLF modified equation carries `D_num = ½αΔξ(1−ν)`, so a front spreads to width `O(√Δξ)`. A **level set** in that tail is `O(Δξ^{1/2})`; an **integral** is `O(Δξ)`. Measured orders 0.45–0.55 and 0.90–0.95 respectively. Halving the `t_br@0.01` error costs 4× the cells, so at `n_xi = 80` (~18 %) reaching 1 % needs ~300×. **No tolerance was relaxed** — the claim that it was a converged number was wrong. | **Yes — `test_a2_threshold_front_is_half_order_and_integrals_are_first_order` asserts the order is in (0.35, 0.75), strictly worse than the L1 order, and that the error exceeds 10 % at `n_xi = 100`** |
| REM-07 (R-1) | Scalar run history | **Recorded every step**, whatever `record_every` says | **Own defect.** | `breakthrough_at` and `efficiency_at` interpolate the recorded history, so sub-sampling made a reported physical quantity depend on a logging setting — 0.0409 vs 0.0537 on ZF22 case 1, a 31 % difference. `run`'s own running detector already guarded against this; the accessor did not. Three floats per step is ~2 MB over a 10⁵-step run. `record_every` now governs only the `StepReport` list and the `on_step` callback. | **Yes — `test_r1_breakthrough_does_not_depend_on_the_logging_frequency` across `record_every ∈ {1, 7, 50, 0}`** |
| REM-08 (BLK-7) | Reading of BF25 (3.13)'s stability range | **Leading edge `c̄₀ → 0`**; BF25's literal `[0,1]` available as `classify(c0_max=1.0)` | **Irreducible ambiguity in the source.** | Taken literally, (3.13) classifies nothing: at `c̄₀ → 1`, `w_f → q₀'(1) + b𝓘₃'(1) = 0` for every pair and every `b` while `w_finger → 1`, so `Δw(1⁻) → +1` unconditionally and all ten ZF22 cases come out "partial penetration" — including case 4 at `b = 1000`, a near-piston. The leading-edge reading is pinned to the paper, not to preference: at `b = 0` it gives `Δw(0⁺) = m − 3/2`, and BF25 §3.2 gives **3/2 as `M₃^min`** from Lajeunesse et al. (1999) by a different argument. Reproduced to 2.3e-9. | **Yes — both readings asserted, and the degeneracy of the literal one asserted explicitly, in `test_bench10_bf25_3_13_literal_range_is_degenerate`** |
| REM-09 (BENCH-10) | Closure state for the Muskat benchmark | `H = 1`, `\|ū\| = 1` | **Explicit in BF25 §3.1**: the closures are computed "imposing the velocity `(v̄, w̄) = (0, 1)`". | Not a choice so much as a transcription, recorded because the table has four axes and picking the wrong slice would be invisible. | n/a |
| REM-10 (R-2) | Resuming a checkpoint | **Refused across a settings change**; a legacy checkpoint warns and proceeds | **Own hazard, found during the UI inventory.** | `Simulation.run` checked only the grid shape, and the checkpoint filename carries six of ~20 settings — so editing a fluid property, or `--cfl`, and relaunching the same command silently continued a field computed under the old physics. `d2dga/runio.py` now records the full `Config` tree, CLI arguments and environment as `<checkpoint>.config.json`, and its `fingerprint` is stored in the checkpoint via `Simulation.run(checkpoint_tag=)`. The fingerprint covers physics and mesh only, *not* the command line, output paths or machine, so re-running the same physics elsewhere still resumes. A tagless legacy checkpoint warns rather than blocking, because refusing would break relaunching the three completed K-GEP-1 runs. | **Yes — `tests/test_runio.py` (9 tests); the two guard tests fail against the previous commit, and the block was verified end to end on `scripts/kgep1_run.py` with `--cfl` changed** |
| REM-11 (G-1) | Non-Newtonian mud in `FluidsConfig` | `mud_consistency`, `mud_power_law_index`, `mud_yield_stress`; `mud_viscosity` becomes a property that **refuses** for a non-Newtonian mud; both fluids validated at construction; `from_record` migrates pre-G-1 payloads and raises on an unknown key | **User request, 2026-09-19** — held until approved because it touches `config.py`. | A Herschel-Bulkley fluid has no single viscosity, so a stored `mud_viscosity` beside a power-law index would be a number in the wrong units waiting to be read. `n > 1` is refused, not extrapolated: BF25 (2.8)-(2.10) derive the closures for shear-thinning HB only. Defaults are bit-identical including `repr`, which matters because `make_table` hashes the scaled fluids into the closure-table cache key. See FLU-07 for what measuring this revealed about B-1. | **Yes — 11 gates in `tests/test_m0_config.py`, including a verbatim reproduction of the pre-G-1 literal construction** |
| REM-12 (G-3) | Live progress out of a run | `--status-json PATH`, written atomically from the existing `on_step`; plus `Simulation.conservation_error`, the running worst volume error, emitted as an attribute | **UI build spec §5**: "if a display needs data the solver does not emit, add an emitting hook, not a calculation in the UI layer." | The alternative was for the monitor to recompute the volume error from `StepReport.mass` and a running flux sum — a **second copy of an invariant**, which is worse than none, because the monitor and the report could then disagree about the same run. The attribute is read-only and nothing in the solver reads it back; UI-5 asserts the live value equals the returned one at every step. The status document is a file because the run must stay a plain subprocess: that is what makes "a UI run is a CLI run" true rather than claimed. | **Yes — UI-5, UI-9** |
| REM-13 | `RunResult.steps` | The total step count, including steps restored from a checkpoint | **Own defect, exposed by the launcher.** | Callers read `res.reports[-1].n`. That is wrong twice: with `record_every > 1` the last *report* is not the last *step*, so every printed count was short by up to `record_every − 1`; and a run relaunched after it had already finished takes **no steps at all**, leaving `reports` empty — so re-running a completed job raised `IndexError` instead of saying it was complete. A launcher does that routinely, because pressing Run twice is free. The runner now also says "nothing to do" and reads the results back rather than pretending to recompute them. | **Yes — UI-6 (the zero-step resume) and UI-7 (`record_every > 1`)** |
| REM-14 (G-4) | Narrow-side efficiency | `postprocess.narrow_side_efficiency(geometry, c, fraction=0.25)` | **Mockup panel with no solver behind it.** | η_E is volume-weighted over the whole annulus and so is dominated by the wide side, which was never in doubt: K-GEP-1 reports 0.9946 overall against a narrow-side minimum of 0.9494. η_N is the *same integral* restricted to the narrowest quarter of φ — not a different measure, the measure the job is about. Put in `postprocess`, not the UI, per spec §5; printed by the runner and recorded in `metrics.json`. | **Yes — M7-T7 pins it to η_E at `fraction = 1`, to the constant on a uniform field, and to zero on a wide-side-only field** |
| REM-15 (G-7/G-8) | Machine-readable gate results, and parsing the ZF22 table | `tests/run_gates.py` (a pytest **plugin**) writes `output/gates.json`; `ui/reports.py` parses `output/zf22_table3.md` | **Own defect, found by looking at the rendered page.** | The Validation screen's first ZF22 parser filtered rows with `startswith("\| ")` and then sliced `[2:]`. The file writes its separator as `\|---\|---\|`, with no space, so the filter removed the separator and the slice removed **case 1** — the only case that does not reproduce. The screen displayed **9 / 9**. Nothing in the code looked wrong; the defect existed only in the rendered output. Parsing moved out of the screen into a module that can be tested without a browser, and the separator is now identified by its *content*. The gates file is never replaced by a fallback to the prose in `gate_status.md`, which is hand-written and cannot go stale loudly. | **Yes — UI-12 (all ten rows, case 1 failing at −44.5%), UI-13 (three separator spellings), UI-14 (an unreadable row is not a pass)** |
| REM-16 | **Conservation verification, BCF25 §IV, as the verification this stage rests on** | `d2dga/conservation.py` — `Vol1(k)` by integrating `c̄_k` over the interior, `Vol2(k)` by accumulating boundary flux, differenced and normalised by the **total annulus volume**; a time series per fluid, plus the total-flux imbalance | **BCF25 §IV verbatim**, including the normalisation. | The codebase already had a conservation number, but it divided by the volume *present*, which → 0 at the start of a run. That is a stricter early-time measure and is kept — but it is **not the quantity BCF25 report**, so it could not be put beside their ~1e-15. Both are now reported and labelled. ⚠️ **What the two-fluid version does and does not prove:** BCF25 evolve `K = 3` concentrations independently, so their three curves are three separate ledgers and "the concentrations sum to 1" is a real result. Here `K = 2` and only `c̄₂` is evolved; `c̄₁ ≡ 1 − c̄₂` pointwise, and the two-fluid closure makes fluid 1's face flux *identically* the total minus fluid 2's — the LLF dissipation changes sign with `c` and cancels exactly. So fluid 1's curve is **not independent evidence**; it differs from `−err₂` only by the total-flux imbalance, which asks whether the elliptic solve delivered the same `Q` at both ends. That imbalance is reported separately rather than hidden inside a second curve that looks like corroboration; measured **exactly 0.0**. | **Yes — 7 gates (M8-T1..T7), including one that injects an unbooked volume and requires the ledger to report it at the size injected, because a conservation check that cannot fail is decoration** |
| REM-17 (A-3b) | **The closure table's velocity axis, and the elliptic Picard iteration** | `[0, 0.005, 0.01, 0.02] + geomspace(0.1, 3000, 15)` — 19 nodes, up from A-3's 17 — and the axis is now **recorded in the run fingerprint** | **Own defect, and a correction to A-3.** | A-3 anchored the axis at zero so an under-range query became impossible, using a single node: `[0, 0.02]`. The note written then said it "changes nothing". **It changed the elliptic solve.** The gap solve at `umag = 0` and `0.02` agrees to six significant figures, so that segment is nearly flat while `[0.02, 0.1]` is not; the kink between them breaks the Picard iteration, which updates `|ū|` from the previous `Ψ` and re-evaluates the mobility — it oscillates across the kink instead of contracting. Measured at 16 × 80 on the K-GEP-1 pair, same field, only the axis changed: `[0.02]` → **6 iterations**, residual 1.54e-09 (but 36 under-range queries per solve); `[0, 0.02]` → **100, at the cap**, residual 3.37e-05; `[0, .005, .01, .02]` → **6**, residual 1.54e-09, **and no query outside the table**. Along a fresh 2982-step trajectory the steps hitting the cap are 37.5 % / 1.5 %; on the live production run, 2.8 % against 37.5 %, and the wall-clock estimate fell from 8.9 h to 2.7 h. ⚠️ **1.5–2.8 % is not the 0 % the pre-A-3 axis achieved**, and I am not claiming the axis is now optimal — only that it is 13–25× better and removes the under-range queries A-3 existed to remove. | **Yes — M9-T1..T4. But read the module docstring: no cheap unit test reproduces the Picard failure itself. Two were tried and both failed to discriminate (a reduced table gives 52 on both axes; a synthetic flat front gives 100 on both). What is gated is the axis and its presence in the fingerprint; the failure is caught at RUN level, where the cap fraction is now stated with a verdict** |
